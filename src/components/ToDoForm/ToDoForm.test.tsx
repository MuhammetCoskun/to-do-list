import React from "react";
import { render, screen } from "@testing-library/react";
import ToDoForm from "./index";

describe("ToDoForm component", () => {
  test("renders input element", () => {
    //arrange
    render(<ToDoForm />);
    //act
    //assert
    expect(false).toBeTruthy();
  });
  test("renders button element", () => {
    //arrange
    render(<ToDoForm />);
    //act
    //assert
    expect(false).toBeTruthy();
  });
  test("captures input value on input change", () => {
    //arrange
    render(<ToDoForm />);
    //act
    //assert
    expect(false).toBeTruthy();
  });
  test("input value should be empty string after button click", () => {
    //arrange
    render(<ToDoForm />);
    //act
    //assert
    expect(false).toBeTruthy();
  });
});
